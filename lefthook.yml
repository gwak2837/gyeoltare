pre-commit:
  jobs:
    - name: biome-staged
      run: pnpm exec biome check --write --staged --no-errors-on-unmatched
      stage_fixed: true

pre-push:
  jobs:
    - name: quality-gate
      run: pnpm run typecheck

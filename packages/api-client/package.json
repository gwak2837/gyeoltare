{
  "name": "@gyeoltare/api-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./browser": "./src/browser.ts",
    "./server": "./src/server.ts"
  },
  "scripts": {
    "build": "pnpm --filter @gyeoltare/api build:types && tsc --project tsconfig.json --noEmit",
    "test": "bun test --pass-with-no-tests",
    "typecheck": "pnpm --filter @gyeoltare/api build:types && tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "hono": "^4.12.18"
  },
  "devDependencies": {
    "@gyeoltare/api": "workspace:*",
    "@gyeoltare/typescript-config": "workspace:*"
  }
}
